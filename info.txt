Deployment process

1. Create new app in Heroku
2. link the app to the github repo


heroku ps:scale web=1 --app isportable
export DYLD_FALLBACK_LIBRARY_PATH=/opt/local/lib:$DYLD_FALLBACK_LIBRARY_PATH
heroku run python --app isportable
http://blog.y3xz.com/blog/2012/08/16/flask-and-postgresql-on-heroku


initialized database schame (run once when deploying the app)

heroku run python --app <appname>
from web import app, db
from importer import clear_data, count, import_data, create_all
create_all(app,db)

Clear all Data (run before importing dat):

heroku run python --app <appname>
from web import app, db
from importer import clear_data, count, import_data, create_all
clear_data(app,db)


Import footprint data:

heroku run python --app <appname>
from web import app, db
from importer import clear_data, count, import_data, create_all
import_data(app,db)


Check the count of footprint data:

heroku run python --app <appname>
from web import app, db
from importer import clear_data, count, import_data, create_all
count(app)

